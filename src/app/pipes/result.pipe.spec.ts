/* tslint:disable:no-unused-variable */

import { ResultPipe } from './result.pipe';

describe('Pipe: MatchResulte', () => {
  it('create an instance', () => {
    let pipe = new ResultPipe();
    expect(pipe).toBeTruthy();
  });
});
